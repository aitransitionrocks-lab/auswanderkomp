import { QuizShell } from "@/components/quiz/QuizShell";

export const metadata = {
  title: "Einschätzung — Auswander-Kompass",
  robots: { index: false, follow: false },
};

export default function DiagnosePage() {
  return <QuizShell />;
}
